export enum Gender {
  Male,
  Female,
}

export enum SubscriptionStatus {
  pending = "pending",
  active = "active",
  expired = "expired",
  cancelled = "cancelled",
}

export enum SessionStatus {
  started = "started",
  ended = "ended",
  cancelled = "cancelled",
}

export enum DessertType {
  drink = "drink",
  meal = "meal",
}
